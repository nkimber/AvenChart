# Base URL of the existing avenchart backend API.
# This app does not run or modify that API — it only calls it over HTTP,
# the same way avenchart/frontend does.
VITE_API_BASE_URL=http://localhost:5001
